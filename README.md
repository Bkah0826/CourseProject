# MealSatisfactionSystem
Update-package -r

update-package Microsoft.CodeDom.Providers.DotNetCompilerPlatform -r


To remove the .dll and .pdb binary files from tracking

git rm --cached *.dll

git rm --cached *.pdb
